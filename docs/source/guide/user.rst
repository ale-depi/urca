User
====

Getting started
---------------

To have fun with URCA locally, try the following steps.

.. code-block:: bash

   $ python3 -m venv venv
   $ source venv/bin/activate
   (venv) $ pip install .
